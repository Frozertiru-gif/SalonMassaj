# Local API run (outside Docker)
# Copy to api/.env for uvicorn/alembic/python scripts.
#
# Чтобы не дублировать десятки одинаковых ключей, здесь оставлены
# только значения, которые обычно отличаются от корневого /.env.
# Остальные переменные (TELEGRAM_*, BACKUP_*, *_TOKENS, LOG_LEVEL и т.д.)
# при необходимости берите из /.env.example.

# Local host for Postgres when API runs on your machine
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/salon

# Keep explicit for clarity in local development
JWT_SECRET=change-me
JWT_EXPIRES_MINUTES=60
TELEGRAM_MODE=webhook

# Optional restore upload size limit in Telegram admin flow
RESTORE_MAX_MB=200
